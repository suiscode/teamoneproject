export const publicRoutes = ["/", "/cars", "/auth/new-verification", '/admin', '/admin/carlist'];

export const authRoutes = [
  "/auth/login",
  "/auth/register",
  "/auth/social",
  "/auth/error",
  "/auth/reset",
  "/auth/new-password",
  '/api/category'
];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/dashboard";
